{
  "name": "testng_node1",
  "version": "1.0.0",
  "description": "Full-stack Node.js application with Express, PostgreSQL, and React",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build:client": "cd client && npm install && npm run build",
    "postinstall": "npm run build:client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}